function changeImageColor() {
    var fileInput = document.getElementById('camara_comercio');
    var image = document.getElementById('img1');

    if (fileInput.files && fileInput.files[0]) {
      image.classList.add('image-green');
    } else {
      image.classList.remove('image-green');
    }
  }


  function changeImageColor14() {
    var fileInput = document.getElementById('SS');
    var image = document.getElementById('img1');

    if (fileInput.files && fileInput.files[0]) {
      image.classList.add('image-green');
    } else {
      image.classList.remove('image-green');
    }
  }

  function changeImageColor1() {
    var fileInput = document.getElementById('Rut');
    var image = document.getElementById('img2');

    if (fileInput.files && fileInput.files[0]) {
      image.classList.add('image-green');
    } else {
      image.classList.remove('image-green');
    }
  }

  function changeImageColor2() {
    var fileInput = document.getElementById('CC');
    var image = document.getElementById('img3');

    if (fileInput.files && fileInput.files[0]) {
      image.classList.add('image-green');
    } else {
      image.classList.remove('image-green');
    }
  }

  function changeImageColor3() {
    var fileInput = document.getElementById('FAA');
    var image = document.getElementById('img4');

    if (fileInput.files && fileInput.files[0]) {
      image.classList.add('image-green');
    } else {
      image.classList.remove('image-green');
    }
  }

  function changeImageColor4() {
    var fileInput = document.getElementById('RC');
    var image = document.getElementById('img5');

    if (fileInput.files && fileInput.files[0]) {
      image.classList.add('image-green');
    } else {
      image.classList.remove('image-green');
    }
  }

  function changeImageColor5() {
    var fileInput = document.getElementById('RRI');
    var image = document.getElementById('img6');

    if (fileInput.files && fileInput.files[0]) {
      image.classList.add('image-green');
    } else {
      image.classList.remove('image-green');
    }
  }

  function changeImageColor6() {
    var fileInput = document.getElementById('GC');
    var image = document.getElementById('img7');

    if (fileInput.files && fileInput.files[0]) {
      image.classList.add('image-green');
    } else {
      image.classList.remove('image-green');
    }
  }
  function changeImageColor7() {
    var fileInput = document.getElementById('AF');
    var image = document.getElementById('img8');

    if (fileInput.files && fileInput.files[0]) {
      image.classList.add('image-green');
    } else {
      image.classList.remove('image-green');
    }
  }

  function changeImageColor8() {
    var fileInput = document.getElementById('ICA');
    var image = document.getElementById('img9');

    if (fileInput.files && fileInput.files[0]) {
      image.classList.add('image-green');
    } else {
      image.classList.remove('image-green');
    }
  }

  function changeImageColor9() {
    var fileInput = document.getElementById('Brochure');
    var image = document.getElementById('img10');

    if (fileInput.files && fileInput.files[0]) {
      image.classList.add('image-green');
    } else {
      image.classList.remove('image-green');
    }
  }

  function changeImageColor10() {
    var fileInput = document.getElementById('CB');
    var image = document.getElementById('img11');

    if (fileInput.files && fileInput.files[0]) {
      image.classList.add('image-green');
    } else {
      image.classList.remove('image-green');
    }
  }

  function changeImageColor11() {
    var fileInput = document.getElementById('SG-SST');
    var image = document.getElementById('img12');

    if (fileInput.files && fileInput.files[0]) {
      image.classList.add('image-green');
    } else {
      image.classList.remove('image-green');
    }
  }

  function changeImageColor12() {
    var fileInput = document.getElementById('Certificado_c');
    var image = document.getElementById('img13');

    if (fileInput.files && fileInput.files[0]) {
      image.classList.add('image-green');
    } else {
      image.classList.remove('image-green');
    }
  }

  function changeImageColor13() {
    var fileInput = document.getElementById('Referencia_comercial2');
    var image = document.getElementById('img14');

    if (fileInput.files && fileInput.files[0]) {
      image.classList.add('image-green');
    } else {
      image.classList.remove('image-green');
    }
  }
